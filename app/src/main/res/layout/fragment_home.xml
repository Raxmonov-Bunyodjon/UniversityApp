<!--
🔹 activity_home.xml

Bu layout fayl Home ekrani uchun asosiy UI ni tashkil qiladi.
Har bir UI elementi funksiyalari bilan to‘liq bog‘langan:
- Toolbar → sarlavha va logout tugmasi
- FragmentContainerView → fragmentlarni joylashtirish (nav host)
- BottomNavigationView → pastki navigatsiya menyusi
-->

<androidx.appcompat.widget.LinearLayoutCompat
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- 🔹 Toolbar -->
    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:background="@color/colorAccent"
        android:gravity="center_vertical"
        android:paddingStart="10dp"
        android:paddingTop="22dp"
        android:orientation="horizontal">

        <!-- 🔹 App sarlavhasi -->
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="18sp"
            android:text="@string/app_name"
            android:textColor="#E91E63"/>

        <!-- 🔹 Logout tugmasi -->
        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/logout"
            android:layout_width="55dp"
            android:layout_height="50dp"
            android:paddingEnd="20dp"
            android:paddingTop="15dp"
            app:srcCompat="@drawable/outline_logout_24" />
    </androidx.appcompat.widget.LinearLayoutCompat>

    <!-- 🔹 FragmentContainerView: pastda fragmentlarni joylashtirish uchun -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/home_nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:navGraph="@navigation/home_nav_graph"
        app:defaultNavHost="false" />

    <!-- 🔹 Pastki navigatsiya menyusi -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorAccent"
        android:elevation="8dp"
        app:itemIconTint="@color/white"
        app:itemTextColor="@color/white"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</androidx.appcompat.widget.LinearLayoutCompat>
